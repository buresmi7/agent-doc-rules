{
  "turns": [
    {
      "id": "request",
      "prompt": "Repair the README and create a concise root `AGENTS.md`.",
      "criteria": {
        "t1": "Repairs `README.md` so verified commands match `package.json`.",
        "t2": "Removes `npm run deploy` and `npm run lint` as supported commands because they are absent from `package.json`.",
        "t3": "Preserves supported commands: `npm test` for tests and `npm run docs:check` for documentation checks.",
        "t4": "Creates a concise root `AGENTS.md` with shared-rule links, source-of-truth guidance, and verification rules.",
        "t5": "Does not invent deployment steps, cloud services, owners, issue workflows, host names, or package scripts.",
        "t6": "Does not create unrelated docs or local skills."
      }
    }
  ]
}
