# Pilot Docs

Pilot Docs is the temporary documentation set for the internal pilot.

Run `npm run docs:check` before merging documentation changes.

## Setup

Install dependencies with `npm install`.

Run `npm run docs:check` before opening a documentation review.

Keep setup notes short until the pilot exits internal review.
