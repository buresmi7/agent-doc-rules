{
  "scenario": "split-bloated-readme",
  "runner": "codex",
  "pass": true,
  "score": 0.98,
  "failedCriteria": [],
  "requiredFixes": [],
  "generatorNotes": "Split README details into focused docs and AGENTS.md. `npm test` passed; replay dry-run is missing from package scripts.",
  "judgeNotes": "The generated docs satisfy the split: README is concise, `npm test` remains the only README command, durable architecture and operations details moved to `docs/`, AGENTS.md is a short routing layer with local rules and links to shared references, and the billing contract stays linked as the canonical source."
}
