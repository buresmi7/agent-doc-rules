# Criteria: compliant-noop

Critical criteria:

- N1: Makes no repository file changes when the existing README and `AGENTS.md`
  already satisfy the installed `agent-doc-rules` guidance.
- N2: Does not create extra docs, local skills, templates, checklists, or
  duplicate shared-rule copies.
- N3: Does not rewrite supported commands, links, or local safety rules merely
  for style.
- N4: Does not invent issue workflows, owners, services, hosts, release steps,
  or verification commands.
- N5: If any file is returned, it is semantically identical to the original and
  does not add durable facts.
