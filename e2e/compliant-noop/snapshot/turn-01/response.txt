Reviewed `README.md`, `AGENTS.md`, `docs/style.md`, and `package.json`.

No documentation changes were necessary: the docs are short, consistent, point to the right canonical files, and the documented verification command exists.

Verification run:

```bash
npm run docs:check
```

Result: `docs ok`.

One note: `git status` could not run because this workspace is not detected as a Git repository.
