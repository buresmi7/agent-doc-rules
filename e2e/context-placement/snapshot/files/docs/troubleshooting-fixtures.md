# Troubleshooting Fixtures

If a fixture fails, run the test suite:

```sh
npm test
```

Inspect the failing row number before changing schema documentation. Schema docs
should describe parser behavior, not hide fixture problems.
