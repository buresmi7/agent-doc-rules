# Beacon Workspace

Beacon Workspace contains a small API package and a CSV importer package.

## Packages

| Path | Content |
| --- | --- |
| `packages/api/` | API notes and endpoint examples. |
| `packages/importer/` | CSV importer for partner files. |

## Verification

Run all tests from the workspace root:

```bash
npm test
```
