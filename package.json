{
  "name": "agent-doc-rules",
  "version": "0.1.4",
  "private": true,
  "description": "Reusable documentation and AGENTS.md rules for AI-assisted repositories",
  "type": "module",
  "scripts": {
    "test": "npm run test:markdown && npm run test:links && npm run test:content && npm run test:audit",
    "test:audit": "node scripts/check-audit.mjs",
    "test:content": "node scripts/check-content.mjs",
    "test:links": "lychee --offline --no-progress -- README.md AGENTS.md CHANGELOG.md rules/*.md templates/*.md",
    "test:llm": "node scripts/llm-review.mjs",
    "test:markdown": "markdownlint-cli2 \"*.md\" \"rules/*.md\" \"templates/*.md\""
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.22.1"
  }
}
